services:
  - type: web
    name: trinetra-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port 10000"
    workingDir: backend
    autoDeploy: true
